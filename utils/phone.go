package utils

// GetPhoneBrand 获取手机品牌
func GetPhoneBrand(device string) string {
	//phoneBrands := config.DefaultConfig.App.DeviceBrands
	//if len(phoneBrands) > 0 {
	//	for _, brand := range phoneBrands {
	//		if pos := strings.Index(strings.ToLower(device), brand); pos >= 0 {
	//			return brand
	//		}
	//	}
	//}
	return "other"
}
